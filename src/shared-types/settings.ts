import { MenuLinkProps } from '../components/MenuLink'

export type Settings = {
  blogName: string
  blogDescription: string
  url: string
  altText: string
  links: MenuLinkProps[]
  footerText: string
}
